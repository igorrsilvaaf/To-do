module.exports = {    env: {        browser: true,  // Adicione se o código roda no navegador        node: true,     // Adicione se o código roda em Node.js        jest: true      // Adiciona o ambiente Jest para testes unitários    },    extends: [        'eslint:recommended',  // Usar configurações recomendadas do ESLint    ],    parserOptions: {        ecmaVersion: 12,        sourceType: 'module',    },    rules: {        // Personalize as regras do ESLint conforme necessário    },};